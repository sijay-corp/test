name: GitHub Actions Demo
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: echo envs
        run: |
          echo ${{ github }}
